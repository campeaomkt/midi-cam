[package]
name = "midi-cam"
version = "1.0.0"
description = "MIDI-Cam Desktop Companion"
authors = ["MIDI-Cam"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
